import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'propnex-investment-suite',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
